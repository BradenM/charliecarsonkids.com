/*
 * {{ properCase name }} Messages
 *
 * This contains all the text for the {{ properCase name }} atom.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.atoms.{{ properCase name }}';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the {{ properCase name }} atom!',
  },
});
