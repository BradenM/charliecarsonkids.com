/**
 *
 * Stories for IconButton
 *
 * Atom
 */

import React from 'react';
import IconButton from '.';

export default {
  title: 'Design|Atoms/IconButton',
  component: IconButton,
};

export const facebookIconButton = () => (
  <IconButton name="facebook" url="https://www.facebook.com" />
);
